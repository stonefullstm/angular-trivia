import { DecodetextPipe } from './decodetext.pipe';

describe('DecodetextPipe', () => {
  it('create an instance', () => {
    const pipe = new DecodetextPipe();
    expect(pipe).toBeTruthy();
  });
});
